# Printmn Web

`printmn-expo` (Konica Minolta native app)-ийн дизайн систем дээр суурилсан
маркетинг + онлайн захиалгын вэбсайт.

## Ажиллуулах

```bash
npm install
npm run dev        # http://localhost:5173 (API-гүй — доорх тайлбарыг үз)
npm run build      # tsc -b && vite build → dist/
npm run verify     # typecheck + test + docs:check
npm run lint
```

Систем бүтэц, захиалгын урсгал, `/pmn` ERD → [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md).

`/api/order`-ыг локалд ажиллуулахын тулд `vercel dev` хэрэглэнэ (`npm i -g vercel`),
эсвэл `.env` дотор `RTDB_AUTH`-ыг тавиад deploy хийгээд шалгана. `vite dev` дангаараа
static файл л үйлчилдэг тул захиалга илгээхэд 404 өгнө.

## Стек

| Зүйл       | Сонголт                                       |
| ---------- | --------------------------------------------- |
| Bundler    | Vite 7 + `@vitejs/plugin-react`               |
| UI         | React 19, TypeScript (strict)                 |
| Загвар     | Tailwind CSS v4 (`@tailwindcss/vite`)         |
| Routing    | react-router-dom 7                            |

## Хуудсууд

| Зам            | Файл                | Тайлбар                                   |
| -------------- | ------------------- | ----------------------------------------- |
| `/`            | `pages/Home.tsx`    | Hero (`Хэвлэл` товч) + холбоо барих        |
| `/hevlel`      | `pages/Print.tsx`   | Хэмжээ, үнэ, зураг оруулах                 |
| `/zakhialga`   | `pages/Order.tsx`   | Мэдээлэл бөглөх, зураг илгээх, төлбөр      |
| `/zakhialga/:дугаар` | `pages/OrderStatus.tsx` | Захиалгын төлөв — буцаж орох хаяг |
| `/tseej-zurag` | `pages/IdPhoto.tsx` | Бичиг баримтын зураг (цэсэнд байхгүй)      |
| `/tseej-zurag/avtomat` | `pages/IdPhotoStudio.tsx` | Ажилтны хэрэгсэл — автомат бэлтгэл |

`/zurag-ugaalt` хуучин зам нь `/hevlel` рүү redirect хийнэ.

Толгойн цэсэнд **Хэвлэл** ба **Цээж зураг** хоёр л байна: вэбээр хийж болох
ажил, дэлгүүр дээр ирж хийлгэдэг ажил. Утсан дээр урт цэс хүний толгойг
эргүүлдэг тул бусад зам (захиалга, холбоо барих) нь урсгалын дотор эсвэл
footer-т амьдарна.

Ажилтны автомат хэрэгсэл рүү `/hevlel` хуудасны цэсний хажуугаас бүдэг
холбоосоор орно — ажилтан өдөржин тэр хуудсан дээр байдаг.

> **Анхаар.** Нэг үе `/tseej-zurag`-ыг цэснээс хассны дараа түүн рүү заасан
> ганц ч холбоос үлдээгүй: хуудас ажилласаар, тест ногоон, гэвч хаягийг гараар
> бичихээс өөр аргагүй байсан. Одоо `test/customer-web.test.ts` нь `App.tsx`
> доторх зам бүрийг шалгаж, `src/` дотроос холбоос олдохгүй бол унана
> (`<Navigate>` redirect-үүдийг зориуд алгасна).

**Энэ бол зөвхөн үйлчлүүлэгчийн вэб.** Ажилтны хуудас байхгүй — захиалгуудын
жагсаалт, зураг татах зэрэг нь `/api/admin` дээр л амьдардаг бөгөөд түүнийг
native app-ын менежерийн хэсэг хэрэглэнэ. Хэрэглэгч зөвхөн **өөрийн** захиалгаа
`/zakhialga/<дугаар>?d=…&u=…` хаягаар, өөрийн нууц түлхүүрээрээ л хардаг.

### `/hevlel` — хэвлэлийн урсгал

1. **Таб** — Угаалт / Засвар / Фото цаас (каталогийн категориуд).
2. **Хэмжээний карт** — `lib/photoSize.ts` нь үйлчилгээний нэрнээс (`10*15`,
   `А4`) хэмжээг таньж, зөв харьцаатай preview, 300dpi-ийн санал болгох
   нягтрал, үнэ, бэлэн болох хугацааг харуулна.
3. **Зураг сонгох** — карт дээр дарахад `PhotoEditor` нээгдэж зураг оруулах,
   хэдэн ширхэг хэвлэхээ сонгоно. **Засварын хэрэгсэл байхгүй** — зураг цаасны
   харьцаанд төвөөрөө автоматаар багтана (`lib/photoRender.ts`), preview дээр
   харагдаж буй зүйл хэвлэгдэх файлтай яг ижил. Нягтрал багавтар байвал
   анхааруулга гарна.
4. **Сагс** — `state/basket.tsx` контекст. Хуудас хооронд хадгалагдана.
5. **Захиалга илгээх** — `/zakhialga` дээр зураг эхлээд R2 руу байршиж, дараа
   нь захиалга Firebase рүү бичигдэнэ ("Зураг хүлээн авах" хэсгийг үз).

`/zakhialga` дээр **каталогийн бусад үйлчилгээ байхгүй**. Өргөмжлөл, медаль,
фудболк зэрэг ажлыг вэбээр захиалдаггүй — утсаар эсвэл дэлгүүр дээр. Вэбийн
цорын ганц зорилго нь зураг хүлээн авах.

Тайралт нь чухал хэсгийг таслах эрсдэлтэй тул **эх файлыг мөн хамт** хадгалдаг
— ажилтан шаардлагатай бол тэндээс дахин бэлдэнэ.

Захиалга илгээх хүртэл зураг браузераас гардаггүй: `URL.createObjectURL` +
canvas-аар зөвхөн дотоод preview үүсгэдэг (`lib/photoRender.ts`).

## Native app-аас юуг хуулсан бэ

Дизайн болон өгөгдөл хоёулаа `printmn-expo`-оос гаралтай тул хоёр платформ
таарч байдаг:

| Web                     | Эх сурвалж (`printmn-expo`)                   |
| ----------------------- | --------------------------------------------- |
| `src/index.css` `@theme` | `src/theme/colors.ts`, `src/theme/theme.ts`   |
| `src/data/catalog.ts`   | `src/constants/catalogDefaults.ts` (82 мөр)   |
| `src/lib/price.ts`      | `src/utils/price.ts`                          |
| `CUSTOM_PRICE_CATEGORIES`, `VAT_RATE` | `src/constants/config.ts`       |

Өнгө: primary `#1a56db`, primaryDark `#1e40af`, accent `#f59e0b`,
surfaceTint `#eff6ff`. Радиус 8/12/16/20px — бүгд native app-тай ижил.

## Back-end — `/api/order`

Захиалга нь native app-тай **яг нэг Firebase Realtime Database** руу очно:
`pmn/orders` + `pmn/worklogs` зангилаанд, `orderLogic.ts` шиг хосоороо.

### Яагаад Vercel function хэрэгтэй вэ

`RTDB_AUTH` бол Firebase-ийн *database secret* — бүрэн admin эрх. Native app-д
энэ нь bundle дотор сууж болдог (app.config.js дээр ч ингэж тайлбарласан
байдаг), гэхдээ вэб дээр bundle бол зүгээр л нэг татаж болох файл. Token-ыг
`VITE_` угтвартай нэрээр тавьбал сайт нээсэн хэн ч DevTools-оос уншаад
`pmn` доторх бүхнийг — ажилтны PIN, орлого, чат — унших, устгах боломжтой.

Тиймээс token зөвхөн серверийн орчны хувьсагчид амьдарч, browser нь
`POST /api/order` руу л хандана.

### Тохируулга

Vercel → Project → Settings → Environment Variables (`.env.example`-ийг үз):

| Хувьсагч                          | Заавал | Тайлбар                                    |
| --------------------------------- | ------ | ------------------------------------------ |
| `RTDB_AUTH`                       | ✅     | Database secret. Байхгүй бол API 503 өгнө  |
| `RTDB_URL`                        | —      | Өгөөгүй бол `…/pmn` руу бичнэ              |
| `R2_ACCOUNT_ID`                   | 🖼     | Cloudflare account id                      |
| `R2_BUCKET`                       | 🖼     | Зургийн bucket-ийн нэр                     |
| `R2_ACCESS_KEY_ID` / `_SECRET_…`  | 🖼     | R2 API token. Object Read & Write          |
| `S3_ENDPOINT` / `S3_REGION`       | —      | Өгвөл R2-ийн оронд өөр S3-нийцтэй сан руу  |
| `ADMIN_TOKEN`                     | 🖼     | `/api/admin`-д хандах токен (native app)   |
| `BANK_NAME` / `_ACCOUNT` / `_HOLDER` | 💳  | Дансаар шилжүүлэх заавар                   |
| `QPAY_USERNAME` / `_PASSWORD` / `_INVOICE_CODE` | — | QPay QR. Байхгүй бол зөвхөн данс |
| `TELEGRAM_BOT_TOKEN` / `_CHAT_ID` | —      | Шинэ захиалгын мэдэгдэл                    |

🖼 = зураг хүлээн авахад. Байхгүй бол доорх «Сангүйгээр ажиллах» хэсгийг үз.
💳 = төлбөрийн заавар харуулахад. Байхгүй бол хэрэглэгчид «утсаар холбогдоно уу»
гэж харуулна, ажилтан гараар баталгаажуулсан хэвээр ажиллана.

### Сангүйгээр ажиллах — R2/NAS-ыг хожим залгах

**Зөвхөн `RTDB_AUTH` заавал.** Объект сан (`R2_*`) тохируулаагүй байсан ч вэб
бүрэн ажиллана:

| Юу болох вэ | Тайлбар |
| --- | --- |
| `/api/upload` | `503` буцаана |
| Клиент | `ServiceUnavailableError` барьж аваад захиалгыг **үргэлжлүүлнэ** |
| `/api/order` | `photos: 'unavailable'` гэж хариулж, захиалгыг Firebase рүү хэвийн бичнэ |
| Хэрэглэгч | «Зураг хүлээн авах систем түр ажиллахгүй байна. Ажилтан залгаж авна» гэсэн мессеж |
| Төлбөр | Дансны заавар ҮРГЭЛЖ харагдана (санаас хамаардаггүй). Автомат хяналт л ажиллахгүй |
| Telegram | «⚠️ N зураг ирсэн ч сан руу орсонгүй» гэж мэдэгдэнэ |

Хожим `R2_*` (эсвэл `S3_ENDPOINT`) хувьсагчдыг Vercel дээр тавиад redeploy
хийхэд л зургийн урсгал автоматаар асна — код өөрчлөх шаардлагагүй.

Энэ нь санамсаргүй унтралтад ч хамаарна: R2 хэдэн минут ажиллахгүй байлаа гэж
вэбээр захиалга авах боломж бүрэн зогсохгүй.

### Локал хөгжүүлэлт — хуурамч back-end

`npm run dev` үед Vercel function-ууд **ажиллахгүй** (тэдгээр нь зөвхөн deploy
хийсний дараа амьдардаг). Тиймээс `dev/mock-api.ts` нь `/api/*`-ыг локал дээр
дуурайдаг Vite plugin — бодит API-тай яг ижил хэлбэрийн хариу өгнө:

- `/api/upload` → хуурамч presigned URL (файл санах ойд хадгалагдана)
- `/api/order` → захиалгын дугаар, дансны заавар, хянах түлхүүр
- `/api/payment` → `pending` → `paid` (20 секундын дараа автоматаар)
- `/api/admin` → жагсаалт. **Ямар ч токен тохирно.** Төлбөрийн түгжээ нь
  бодит API-тай ижил ажиллана — төлөөгүй захиалганд линк буцаадаггүй

```bash
npm run dev                    # төлбөр 20 секундын дараа автоматаар орно
MOCK_AUTOPAY_MS=0 npm run dev  # зөвхөн /api/admin-аар гараар баталгаажуулна
```

`apply: 'serve'` тул үйлдвэрлэлийн build-д ОРОХГҮЙ. `test/mock-api.test.ts` нь
хуурамч back-end бодит API-ийн гэрээг зөрчөөгүйг шалгана.

### App дээр хаана харагдах вэ

**Захиалга** таб (manager / ipad эрхтэй хүнд; worker-т энэ таб байхгүй).
`OrdersScreen` нь `orders` биш **`worklogs`**-ыг рендэрлэдэг — бид хоёуланд нь
бичдэг тул өдрийн касс (`orders`) болон самбар (`worklogs`) хоёулаа таарна.

Хоёр нарийн зүйл дээр анхаарсан:

- **Огноо Улаанбаатарын цагаар.** Vercel UTC-ээр явдаг тул серверийн локал
  огноог авбал өглөөний 00:00–08:00-д ирсэн захиалга өмнөх өдрөөр бичигдэж,
  `visibleWorkLogs`-ийн `date === today` шүүлтүүрээс унаж, самбарт огт
  харагдахгүй байх байсан.
- **`no` дараалал.** `newestFirst` эхлээд `no`-гоор эрэмбэлдэг тул дугааргүй
  мөр 0 болж жагсаалтын доод талд ордог. Function өнөөдрийн worklog-ийг
  индексжсэн асуултаар тоолж дугаарыг үргэлжлүүлдэг.

Мэдэгдэх хугацаа: `DataContext` 30 секунд тутам татдаг (`SYNC_INTERVAL_MS`),
тиймээс шинэ захиалга ~30 секундын дотор гарч ирнэ. Апп-ын бичилтүүд бүгд
record-level PUT (`writeChild`) учир бүтэн коллекцийг дарж вэб захиалгыг
устгах эрсдэлгүй.

### Function юу хийдэг вэ

- **Үнийг сервер дээр дахин тооцно.** Клиентийн явуулсан `unitPrice`-ыг
  зөвхөн тохиролцооны категорид (`Медаль & Цом`, `Хувцас хэвлэл`), хязгаарын
  дотор байвал л хүлээж авна. Эс тэгвээс 500₮-ийн ажлыг 1₮ болгож захиалж болно.
- **Нэг атомик multi-path PATCH.** Order, WorkLog хоёрыг тусад нь PUT хийвэл
  эхнийх нь орж, хоёр дахь нь унаж, орлого чимээгүй алдагдах эрсдэлтэй.
- **id давхцлаас сэргийлнэ** (`utils/id.ts`-ийн `nextId` логик) — id нь Firebase
  хүүхэд зам учир давхцвал нэг бичлэг нөгөөгөө дарна.
- Хүргэлт/НӨАТ-ыг зөвхөн эхний worklog дээр тэмдэглэнэ (тайланд давхар тоологдохгүй).
- IP-д суурилсан хөнгөн rate limit. Edge instance бүр өөрийн санах ойтой тул
  энэ нь баталгаа биш — жинхэнэ хэрэгцээ гарвал Upstash Redis / Vercel Firewall.

`npm test` нь 50 тест ажиллуулна: үнийн тооцоо, баталгаажуулалт, Firebase
rules-ийн `.validate` шаардлагууд, SigV4 гарын үсэг, хадгалалтын порт, мөн
handler-ийн бүтэн зам (хуурамч Firebase сервер дээр).

## Зураг хүлээн авах — Cloudflare R2

Гол зорилго: хэрэглэгч зургаа вэб дээр оруулна, ажилтан татаж аваад зургийн
машинд өгнө. Зураг Firebase рүү ОГТ ордоггүй — R2 (S3-тэй нийцтэй) санд очно.

### Урсгал

```
Браузер                     Vercel edge                  Cloudflare R2
   │ 1. canvas: 300dpi хэвлэлийн JPEG
   │ 2. POST /api/upload  ──────────►  presigned PUT URL үүсгэнэ ──►
   │ 3. PUT зураг ─────────────────────────────────────────────────► uploads/…
   │ 4. POST /api/order   ──────────►  Firebase: orders + worklogs
   │                                   R2:       manifests/….json
Ажилтан
   │ 5. GET /api/admin    ──────────►  manifest унших + presigned GET
   │    (native app-ын менежерийн хэсгээс, x-admin-token-оор)
   │ 6. Зургаа татаж авна ◄────────────────────────────────────────── uploads/…
```

**Файлыг сервер дундуур оруулдаггүй.** Vercel-ийн хүсэлтийн бие 4.5MB-аар
хязгаарлагдсан бөгөөд утсаар авсан нэг зураг л ихэвчлэн үүнээс том. Сервер
зөвхөн 20 минут амьдардаг presigned URL үүсгэж, браузер R2 руу шууд PUT хийнэ.

**Түлхүүрийг зөвхөн сервер тодорхойлно.** `uploads/<огноо>/<uploadId>/01-print.jpg`.
Клиентээс ирсэн замд итгэвэл дурын хүн бусдын зургийг дарж бичих боломжтой.
`/api/order` дээр түлхүүр бүр тухайн `uploadId`-ийн угтвартай эсэхийг шалгана —
эс тэгвээс хэн ч өөр захиалгын зургийг өөрийн manifest-даа оруулж татах байсан.

**Зураг бүрээс хоёр файл гарна:** `print` (сонгосон хэмжээний харьцаагаар
тайрсан, засвар тусгасан, 300dpi — шууд хэвлэнэ) ба `original` (эх файл —
тайралт буруу байвал эсвэл өөр хэмжээгээр дахин хэвлэхэд).

**Индекс нь R2 дотроо.** `manifests/<огноо>/<захиалгын дугаар>-<uploadId>.json`
дотор хэрэглэгчийн мэдээлэл, мөрүүд, файлын жагсаалт бүгд байна. Firebase-ийн
`database.rules.json` нь native app-ын мэдэлд байдаг тул шинэ зангилаа нэмэх нь
rules-ээс татгалзах эрсдэлтэй — оронд нь зураг ба индексийг нэг газар барив.
Firebase рүү зөвхөн worklog-ийн `note` дээр `🖼 N зураг вэбээр ирсэн` гэж бичнэ.

### R2 bucket тохируулах

1. Cloudflare → R2 → **Create bucket** (жишээ нь `printmn-photos`, байршил APAC).
2. **CORS policy** нэмнэ — эс тэгвээс браузерын PUT-ыг хөтөч блоклоно:

   ```json
   [
     {
       "AllowedOrigins": ["https://printmn.mn", "http://localhost:5173"],
       "AllowedMethods": ["PUT", "GET"],
       "AllowedHeaders": ["content-type"],
       "MaxAgeSeconds": 3600
     }
   ]
   ```

3. **Manage API tokens** → Object Read & Write, зөвхөн энэ bucket. Гарсан
   `Access Key ID` / `Secret Access Key`-г Vercel-д тавина.
4. Bucket-ыг **нийтэд нээхгүй**. Бүх хандалт presigned URL-ээр явна.
5. Санамсаргүй өсөлтөөс сэргийлж **lifecycle rule** нэмэхийг зөвлөнө:
   `uploads/` угтвар — 90 хоногийн дараа устгах.

### Өөр сан руу шилжих (NAS, MinIO, B2, S3)

`api/_r2.ts` нь ердийн SigV4 гарын үсэг бичдэг тул **дурын S3-нийцтэй сан**
дээр ажиллана. Код өөрчлөх шаардлагагүй — зөвхөн орчны хувьсагч:

```
S3_ENDPOINT=https://s3.printmn.mn:9000
S3_REGION=us-east-1
R2_BUCKET=photos
R2_ACCESS_KEY_ID=…
R2_SECRET_ACCESS_KEY=…
```

Дэлгүүрийн NAS дээр MinIO (Docker) ажиллуулбал зураг байрандаа очно. Гэхдээ
NAS-ыг интернэтэд гаргах нь тогтмол IP, HTTPS сертификат, урсгал хамгаалалт
шаарддаг бөгөөд дэлгүүрийн интернэт эсвэл цахилгаан унахад вэбийн зураг
хүлээн авах хэсэг бүхэлдээ зогсоно.

**Илүү аюулгүй хослол:** зураг R2 руу очиж, NAS нь R2-оос **татаж авч**
архивлана (`rclone sync` эсвэл Synology Cloud Sync, 10 минут тутам). Ингэхэд
ажилтан NAS дээрх дотоод хавтаснаас шууд хэвлэнэ, вэб нь дэлгүүрийн сүлжээнээс
хамаарахгүй, R2 дээрх хуулбарыг 30–90 хоногийн дараа автоматаар устгана.

```bash
# NAS дээрх cron — 10 минут тутам
rclone sync r2:printmn-photos/uploads /volume1/photo/web-orders \
  --create-empty-src-dirs --log-file=/volume1/logs/rclone.log
```

## Төлбөр — зураг төлбөр орсны дараа нээгдэнэ

**Дүрэм: төлбөр баталгаажтал ажилтан зургийг харах ч, татах ч боломжгүй.**

Файл нь захиалга илгээмэгц R2 руу шууд байршдаг — тэгэхгүй бол хэрэглэгч банкны
апп руу шилжих үед браузер хаагдвал зураг алдагдана. Харин **түгжээ нь линк
олгох үе шат дээр** байдаг: `/api/admin` төлөөгүй захиалганд presigned URL ОГТ
үүсгэдэггүй. Интерфейс дээр товч идэвхгүй болгох нь хамгаалалт биш — DevTools
нээсэн хэн ч тойрч гарна.

### Хоёр зам

| Зам | Тохиргоо | Баталгаажилт |
| --- | --- | --- |
| Данс руу шилжүүлэг | `BANK_NAME`, `BANK_ACCOUNT`, `BANK_HOLDER` | Ажилтан `POST /api/admin {action:'pay'}` дуудна |
| QPay (QR + банкны апп) | `QPAY_USERNAME`, `QPAY_PASSWORD`, `QPAY_INVOICE_CODE` | Автомат — QPay callback |

Хоёуланг зэрэг асааж болно: хэрэглэгч QR уншуулах эсвэл данс руу шилжүүлэхээ
өөрөө сонгоно. Аль нь ч тохируулаагүй бол утсаар холбогдох заавар гарна.

### QPay-ийн урсгал

```
/api/order  →  POST /v2/auth/token  →  POST /v2/invoice
                                            ↓  QR + deeplink
                                       хэрэглэгч төлнө
                                            ↓
QPay → /api/qpay-callback?order=…&date=…&u=…&payment_id=…
                                            ↓
                          POST /v2/payment/check  ← ЗААВАЛ
                                            ↓
                              manifest.payment.status = 'paid'
```

⚠️ **`payment/check`-ийг алгасаж болохгүй.** Callback бол нийтэд нээлттэй HTTP
хаяг — хэн ч дуудаж «төлөгдлөө» гэж бичүүлэх боломжтой. Зөвхөн QPay-ээс өөрөөс
нь асууж баталгаажуулсан үед л төлбөрийг хүлээн зөвшөөрнө. Давхар мэдэгдэл
ирвэл юу ч өөрчлөгдөхгүй (аль хэдийн төлөгдсөн manifest-ыг дахин бичихгүй).

Хэрэглэгчийн талд `/api/payment` нь 5 секунд тутам төлөв асууна — банкны
аппаасаа буцаж ирмэгц дэлгэц өөрөө «Төлбөр баталгаажлаа» болно. Нэвтрэлт нь
`uploadId` (16 тэмдэгт санамсаргүй), хариунд хувийн мэдээлэл ОГТ ордоггүй.
QPay «cron-оор байнга шалгахыг хориглоно» гэсэн тул сервер нэхэмжлэл бүрийг
20 секундэд нэгээс олон удаа шалгадаггүй.

### Telegram мэдэгдэл

`TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` тавибал **хоёр** агшинд мэдэгдэнэ:

| Хэзээ | Мессеж |
| --- | --- |
| Захиалга ирэхэд | `🌐 Вэбээс шинэ захиалга! … 🖼 3 зураг — ⏳ төлбөр хүлээгдэж байна` |
| Төлбөр орсонд | `✅ Төлбөр орлоо! … 🖼 3 зураг татахад бэлэн` |

Хоёр дахь нь **чухал**: «төлбөр эхэлж» гэсэн дүрэмтэй болсон учир зураг яг энэ
мөчид түгжээгээ тайлдаг. Мэдэгдэлгүй бол ажилтан жагсаалтаа байнга сэргээж
хараад суух хэрэгтэй болно. QPay callback, `/api/payment` шалгалт, гараар
баталгаажуулалт — гурвуулаа мэдэгдэл илгээнэ.

**Тохируулах:** BotFather-аас бот үүсгэж token авах → ботоо дэлгүүрийн групп рүү
нэмэх → групп рүү мессеж бичээд `https://api.telegram.org/bot<TOKEN>/getUpdates`
дуудаж `chat.id` авах (групп бол хасах тоо).

### Захиалгын төлөв — `/zakhialga/<дугаар>?d=…&u=…`

Баталгаажуулалт нь түр зуурын дэлгэц биш, **буцаж орж болдог хаяг**. Ингэснээр
хуудсаа сэргээхэд захиалга алдагдахгүй, төлбөрөө маргааш төлж болно, линкээ
хуваалцаж өөр хүнээр төлүүлж ч болно.

Хаягийг мэдэх нь хангалтгүй: `u` (uploadId, 16 тэмдэгт санамсаргүй) байхгүй бол
сервер өгөгдөл буцаадаггүй тул дугаарыг таасан ч өөр хүний захиалгыг харах
боломжгүй. Хариунд и-мэйл, тайлбар зэрэг илүү мэдээлэл ОРОХГҮЙ.

Баримт нь `localStorage`-д 14 хоног хадгалагдаж, нүүр болон `/hevlel` дээр
«Сүүлийн захиалга» мөр болж гарна — хэрэглэгч линкээ хаясан ч олно.

### Давхар захиалгаас хамгаалах

Клиент илгээх оролдлого бүрт `requestId` үүсгэнэ; сүлжээ унаж дахин илгээвэл
ижил утга явна. Сервер тухайн id-г 10 минут санаж, давтагдсан хүсэлтэд **шинэ
захиалга үүсгэхгүй** — өмнөх хариугаа буцаана.

Үүнгүй бол сүлжээ удаашрахад дахин дарсан хэрэглэгч Firebase-д хоёр захиалга
бичүүлж, өдрийн касс болон ажлын самбар давхардаж, ажилтан хоёр удаа хэвлэх
эрсдэлтэй. (Edge instance бүр өөрийн санах ойтой тул бүрэн баталгаа биш —
жинхэнэ баталгаа хэрэгтэй бол Upstash Redis.)

### `/api/admin` — ажилтны API (вэб дээр интерфейс БАЙХГҮЙ)

Энэ бол үйлчлүүлэгчийн вэб учир **захиалгуудын жагсаалт вэб дээр огт
харагдахгүй**. Ажилтны интерфейс нь native app-ын менежерийн хэсэгт байрлана —
энд зөвхөн API үлдсэн.

Бүх дуудлагад `x-admin-token: <ADMIN_TOKEN>` толгой шаардана.

| Дуудлага | Үр дүн |
| --- | --- |
| `GET /api/admin?days=7` | Сүүлийн захиалгууд. Төлөгдсөн бол `files[].url` (1 цаг), төлөөгүй бол `null` |
| `POST /api/admin {action:'pay', ref, paid:true}` | Төлбөр гараар баталгаажуулах. Telegram руу мэдэгдэнэ |
| `POST /api/admin {action:'mark', ref, printed:true}` | «Хэвлэсэн» гэж тэмдэглэх |

`ref` нь `GET`-ийн хариунд ирдэг (`manifestKey` талбар).

```bash
# Ажилтан гар аргаар шалгах жишээ
curl -s https://printmn.mn/api/admin?days=1 -H "x-admin-token: $ADMIN_TOKEN"
```

⚠️ **Төлбөр баталгаажаагүй захиалганд татах линк ОГТ буцаадаггүй** — түгжээ нь
интерфейст биш, линк олгох үе шат дээр байдаг.

## Гүйцэтгэл

Зорилтот хэрэглэгч бол **гар утас, 4G, дунджаас доогуур төхөөрөмж**. Тиймээс
байт хэмнэхээс илүү *үндсэн урсгалыг чөлөөтэй байлгах* тал дээр анхаарсан.

**Замын түвшний хуваалт.** Нүүр хуудсанд каталог (17KB), захиалгын маягт,
зургийн код, ажилтны хуудас хэрэггүй. `React.lazy` ашиглан анхны JS-ийг
~92KB → ~62KB gzip болгосон. Салангид байснаас үүсэх цорын ганц сул тал —
товч дарахад хүлээх — `requestIdleCallback` дээрх урьдчилсан татаж авалтаар
арилсан (`usePrefetchMainRoutes`).

**Гуравдагч сан тусдаа chunk-д.** React, react-router бараг өөрчлөгддөггүй тул
`vendor` файлд гаргасан — бидний код шинэчлэгдэх бүрт хэрэглэгч дахин татахгүй.

**Зургийг үндсэн урсгал дээр задлахгүй.** Утасны 12MP зураг ~50MB битмап
үүсгэдэг. `<img src=…>` бол үүнийг үндсэн урсгал дээр задалж интерфейсийг
царцаадаг. Оронд нь `createImageBitmap` (урсгалаас гадуур) ашиглаж, ажил
дуусмагц `close()`-оор санах ойг нэн даруй чөлөөлнө.

**Эх файл DOM-д хэзээ ч ордоггүй.** Зураг сонгосон даруйд 640px-ийн жижиг
хувилбар үүсгээд интерфейст зөвхөн түүнийг харуулна. Сагсанд 20 зураг байсан ч
санах ойд хэдхэн MB эзэлнэ (`File` заагч + жижиг preview).

**Захиалга илгээх үе хоёр үе шаттай.** `prepare` (хэвлэлийн JPEG бэлдэх) ба
`upload` (сүлжээ) — хоёулаа явцын мөрөнд харагдана. Зураг бүрийн дараа
хөтөчид зурах завсар өгдөг тул олон зурагтай захиалгад ч интерфейс царцахгүй.

**`vercel.json`** нь SPA-гийн гүн линкийг (`/hevlel` дээр F5 дарах) зөв
шийдэж, `/assets/*`-д нэг жилийн `immutable` кэш ба аюулгүй байдлын толгойнууд тавьдаг.

**Тасарсан байршуулалтыг дахин оролдоно.** Гар утасны сүлжээ богино хугацаанд
тасрах нь энгийн үзэгдэл. Файл бүрийг backoff-той 3 удаа оролдоно; `4xx`
(гарын үсэг хугацаа дууссан) дээр давтахгүй — утгагүй. Үүнгүй бол 20 зурагтай
захиалга 19 дэх дээрээ унаж, хэрэглэгч бүхнийг эхнээс нь давтах хэрэгтэй болно.

**Маягтын жижиг зүйлс.** `autocomplete`/`inputmode`/`type="tel"` — утас өөрөө
бөглөж, зөв гар гаргана. Утасны талбар зөвхөн цифр авна (хэрэглэгч зай, зураас
бичсэн ч шалгалтад унахгүй). Алдаатай илгээвэл эхний алдаатай талбар руу
автоматаар гүйлгэж фокус өгнө — утсан дээр алдааны текст дэлгэцээс гадуур
үлдэж, хэрэглэгч эргэлзэх нь түгээмэл. Зураг илгээж байх үед хуудас хаахаас
сэрэмжлүүлнэ.

## Өгөгдлийн сан солиход бэлэн

Захиалгын индекс, төлбөрийн төлөв нь одоо R2 дээрх JSON manifest. Handler-ууд
үүнийг МЭДЭХГҮЙ — тэд зөвхөн `api/_store/` доторх портыг хардаг:

```
api/_store/
  types.ts     — WebOrderStore интерфейс (save / get / list / update / fileUrl)
  r2Store.ts   — одоогийн хэрэгжүүлэлт (объект сан)
  index.ts     — getStore(): аль хэрэгжүүлэлтийг сонгохыг ГАНЦ газар шийднэ
```

**Postgres руу шилжих:**

1. `psql "$DATABASE_URL" -f docs/schema.sql`
2. `api/_store/postgres.ts` дотор `WebOrderStore`-ыг хэрэгжүүлнэ
3. `index.ts` дээр `if (env.DATABASE_URL) return createPostgresStore(env);`
4. Хуучин manifest-уудыг нэг удаагийн script-ээр `INSERT` хийнэ

`order.ts`, `admin.ts`, `payment.ts`, `qpay-callback.ts` болон тестүүд
өөрчлөгдөхгүй.

**Зураг өгөгдлийн санд ОРОХГҮЙ.** Хэдэн MB-ийн JPEG нь нөөцлөлт, репликацийг
үнэтэй болгодог бөгөөд presigned URL-ээр браузераас шууд байршуулах давуу
талыг алдуулна. Хүснэгтэд зөвхөн `storage_key` үлдэнэ.

**Хэзээ шилжих вэ:** утсаар хайх хэрэгтэй болох, өдөрт 200+ захиалга болж
жагсаалт удаашрах, орлогын тайлан хэрэгтэй болох, эсвэл хоёр ажилтан зэрэг
тэмдэглэхэд өөрчлөлт алдагдах үед. ER диаграм ба дэлгэрэнгүй:
[`docs/ARCHITECTURE.md` §5.1](./docs/ARCHITECTURE.md), [`docs/schema.sql`](./docs/schema.sql).

## Дэлгүүрийн мэдээлэл — `src/data/site.ts`

Утас, хаяг, ажлын цаг, и-мэйл, сошиал, түгээмэл асуулт **бүгд нэг файлд**.
Толгой, хөл, холбоо барих хэсэг, захиалгын дэлгэцүүд бүгд эндээс уншдаг тул
дугаараа солиход нэг мөр засахад хангалттай.

⚠️ Одоогийн утгууд нь **жишээ** — ашиглалтад гаргахаас өмнө солино:
`phones`, `emails`, `address.full`, `address.mapUrl`, `hours`, `social`.

Бүтэц нь [ppm32.mn](https://ppm32.mn) зэрэг монголын хэвлэлийн газруудын
хэв маягийг дагасан:

- **Толгойд утасны дугаар** — «энэ хэмжээ байна уу», «хэзээ бэлэн болох вэ»
  гэсэн богино асуултад хүн вэб тэнүүчлэхээс залгах нь хурдан. Утсан дээр
  дугаарын товч цэс нээхгүйгээр шууд харагдана.
- **Ажлын цаг мөр мөрөөр** — «Да–Ба: 9:00–18:00» гэсэн ганц мөр нь бямбад
  нээлттэй эсэхийг хэлдэггүй тул хүн залгаж асуудаг.
- **Газрын зураг** — байршил: `Konica Minolta Нарантуул салбар`
  (47.9086607, 106.9485525). «Энэ хаяг хаана байдаг вэ» гэсэн дуудлагыг
  үүнээс өөр юу ч бууруулдаггүй.

  ⚠️ Google Maps-ийн `<iframe>` нь **~1MB JS** татдаг тул `MapEmbed.tsx` нь
  **facade** хэв маягтай: эхлээд хөнгөн хайрцаг харуулж, «Газрын зураг харах»
  дарсан үед л iframe ачаална. Ихэнх хүн үүнийг дардаггүй — тэдэнд зөвхөн
  хаяг, чиглүүлэх товч хэрэгтэй.

  Embed-д товч линк (`maps.app.goo.gl`) биш **координат** ашигладаг: товч
  линк дотроо чиглүүлэлт хийдэг тул хөтөч iframe дотор блокложл, мөн
  координат нь линк хүчингүй болсон ч ажилласаар байна.
- **Баялаг хөл** — байгууллага, утас, хаяг, цаг дөрвөн багана. Өмнө нь зөвхөн
  copyright мөр байсан.
- **Түгээмэл асуулт** — хэзээ бэлэн болох, өнгө таарах эсэх, ямар зураг
  тохирох, зураг хаана хадгалагдах. `<details>` учир JS-гүй ажиллаж, хайлтын
  систем ч уншина.

## Цээж зураг автоматжуулах — `/tseej-zurag/avtomat`

Зураг оруулахад **нүүрийг өөрөө олж**, арын дэвсгэрийг цагаан болгож,
баримтын стандартын дагуу тасалж, **300dpi-аар хэвлэхэд бэлэн ганц файл**
гаргана. Ажилтан одоо үүнийг Photoshop дээр 5–10 минутаар хийдэг.

**Хэмжээ:** 3×4, 3.5×4.5, 4×6, 2×3 см.

| Алхам | Юу болох вэ |
| --- | --- |
| Задлалт | `createImageBitmap`, 480px ажлын хуулбар үүсгэнэ (хурдны төлөө) |
| Нүүр олох | `lib/faceDetect.ts` → толгойн орой, өргөн, төв |
| Тайралт | `cropForFace()` — толгойн өндөр = `headRatio`, орой нь `topMargin` зайд |
| Дэвсгэр | Цагаан / цайвар цэнхэр / саарал / **Хэвээр** |
| Гаралт | 10×15 (1181×1772 px) дээр торлон, зүсэх зааврын хүрээтэй JPEG |

**Захиалгын мэдээлэл ЭНД ОГТ БАЙХГҮЙ.** Бүх боловсруулалт canvas дээр, сервер
рүү юу ч илгээгддэггүй. Цэсэнд ороогүй, `noindex`.

### Хязгаарлалт

**Нүүр илрүүлэлт** нь дүрсийн хүрээнд (silhouette) суурилсан. Жигд дэвсгэр
дээр, урдаас, гэрэлтэй авсан зурагт сайн; эмх замбараагүй дэвсгэр, хэт хажуу
эргэсэн зурагт алдана. Нарийвчлал хэрэгтэй бол `detectFace`-ийг **MediaPipe
Face Mesh**-ээр солиход бусад код өөрчлөгдөхгүй — порт нь `FaceDetector` гэрээ.

**Арын дэвсгэр авахад** буржгар үс, нимгэн шил зэрэг нарийн ирмэг заримдаа
бүдгэрнэ. Хүнд тохиолдолд **«Хэвээр»** сонгоод гараар засах нь хурдан.

#### Дэвсгэр салгах загвар — гурван механизм

Эхний хувилбар дэвсгэрийг **ганц тогтмол өнгө** гэж үзэж, зарим хэсгийг
дутуу арилгадаг байв. Гурван зүйлийг нэмсэн:

**1. Орон зайн загвар** (`fitBackdrop`). Ирмэгийн түүврээр суваг тус бүрт
хавтгай тааруулна: `утга(x,y) = a·x + b·y + c`. Пиксел бүрийг ТУХАЙН ЦЭГИЙН
таамаглалтай харьцуулна, ерөнхий дундажтай биш. Нэг талын гэрэлтүүлэг,
булангийн харанхуйлалт энд шингэнэ.

400×533 зураг, тэвчээр 40 дээр хэмжсэн (арилгаагүй үлдсэн пиксел):

| Гэрлийн уналт | Тогтмол өнгө | Орон зайн загвар |
| --- | --- | --- |
| 0–40 нэгж | 0 | 0 |
| 60 нэгж | **46 904** (22%) | 0 |
| 80 нэгж | **88 155** (41%) | 0 |

Хоёр загварын аль нь ч хүнийг илүү идээгүй (0 пиксел).

**2. Гистерезис.** Хоёр шалгуурыг зэрэг хангана: таамагласан дэвсгэрээс
`tolerance` дотор байх (ерөнхий баталгаа) БА ирсэн хөршөөсөө `step` дотор
байх (орон нутгийн тасралтгүй байдал). Ганц ерөнхий хязгаартай бол сүүдэр
орхигдоно; ганц орон нутгийнхтай бол зөөлөн ирмэгээр хүн рүү нэвчинэ.

**3. Хаалттай завсрыг нөхөх.** Үерийн дүүргэлт зөвхөн зургийн ирмэгээс
эхэлдэг тул үсний хооронд харагдах гадна дэвсгэр хөндөгдөхгүй үлддэг.

Энд нэг **нарийн урхи** бий: үсний завсар ба нүдний цагаан хоёр хоёулаа
жижиг, хаалттай, дэвсгэртэй ижил өнгөтэй. Өнгө, хэмжээгээр ялгах
БОЛОМЖГҮЙ. Ялгарах шинж нь геометр — завсар нь нимгэн үсний цаана,
дэвсгэрээс хэдхэн пикселийн зайд; нүд бол нүүрний гүнд. Тиймээс баталгаатай
дэвсгэрээс `reach` алхмын дотор БҮГД нь багтсан завсрыг л нөхнө.

Энэ хамгаалалт алдвал хэвлэсэн зураг дээр хүний нүд цоорно —
`test/idPhoto.test.ts` дотор хос тестээр түгжсэн.

Дэвсгэр эмх замбараагүй бол (`uniform: false`) хавтгайд итгэхгүй, тогтмол
өнгө рүү буцаад ажилтанд сануулга харуулна.

300dpi гаралт (1181×1772) дээр нийт 242ms — хүлээлгэдэггүй.

**Нүүр олдоогүй үед зориуд таслахгүй.** Буруу таслагдсан зураг хэвлэгдснээс
дахин авахыг хүссэн нь хямд. Илрүүлэлт эргэлзээтэй (`confidence: 'low'`) бол
таслах ч сэрэмжлүүлж, шалгахыг хүснэ.

### AI загвар — юуг авч, юуг зориуд аваагүй вэ

Бүтэн AI pipeline (MTCNN → BiRefNet → GFPGAN → Real-ESRGAN → IDM-VTON) санал
болгогдсон. Зургаан блокоос **нэгийг** нь авсан. Шалтгаан нь техникийн биш.

| Блок | Шийдвэр | Учир |
| --- | --- | --- |
| Нүүр илрүүлэх | Дүрсийн хүрээ хэвээр | InsightFace-ийн бэлэн жин арилжаанд хориотой |
| **Дэвсгэр салгах** | ✅ **U²-Net авсан** | Apache 2.0, браузерт багтана, асуудлыг үнэхээр шийднэ |
| Нүүр сайжруулах | ❌ Аваагүй | CodeFormer арилжаанд хориотой; үүсгэгч — царай өөрчилнө |
| Өнгө, гэрэл | ⚠️ Зөвхөн цагаан баланс | Үүсгэгч бус хэсгийг нь авсан (доор) |
| Хувцас солих | ❌ Аваагүй | IDM-VTON арилжаанд хориотой; баримтад зарчмын асуудал |
| Чанарын оноо | Хэсэгчлэн | `confidence`, `uniform` тугаар аль хэдийн бий |

#### Лиценз

Дэлгүүр бол **худалдааны** байгууллага. Дараах загваруудын жинг арилжаанд
ашиглах эрхгүй: [CodeFormer](https://github.com/sczhou/CodeFormer/blob/master/LICENSE)
(S-Lab 1.0), [IDM-VTON](https://huggingface.co/yisol/IDM-VTON) (CC BY-NC-SA 4.0),
[InsightFace-ийн бэлэн жин](https://github.com/deepinsight/insightface/blob/master/python-package/README.md)
(судалгааны зориулалт).

InsightFace-ийн **код** нь MIT — хориотой нь **жин**. `test/customer-web.test.ts`
эдгээр нэрийг кодоос хайж түгжсэн.

#### Баримт vs энгийн зураг

Иргэний үнэмлэх, паспортын зураг бол хүнийг таних баримт. Царай, хувцсыг
өөрчилсөн зураг тавих нь баримт гуйвуулсан хэрэг бөгөөд эрсдэл нь **зураг
авсан дэлгүүр дээр** буудаг. CV, самбарын зурагт ижил засвар огт асуудалгүй.

Тиймээс ялгаа нь загварт биш **хэрэглээнд** байна — `Purpose.allowRetouch`
үүнийг албадан барина. Анхдагч нь `document` (эрсдэлгүй тал).

Цагаан балансыг зөвшөөрсөн горим ялгаагүй: энэ нь суваг тус бүрт ганц
коэффициент үржүүлдэг **үүсгэгч бус** засвар. Дэвсгэр нь саарал байх ёстой
гэдгийг мэддэг тул түүнийг гэрэл зургийн «саарал карт» болгон ашиглана.
Царайны бүтэц хөндөгдөхгүй.

#### Загвар суулгах

`public/models/u2netp.onnx` — [дэлгэрэнгүй](public/models/README.md).
Хоосон байж болно: загваргүй үед дүрсийн хүрээний арга ажиллана,
`onnxruntime-web` нь **татагдахгүй** (код эхлээд `HEAD`-ээр файл шалгана).

### Яагаад Haar cascade биш вэ

Анхны төлөвлөгөө OpenCV-ийн Haar cascade байсан. Гэвч энэ хэрэгсэл **студийн
зураг** дээр ажилладаг бөгөөд дэлгүүрийн өөрийн шаардлага нь «цагаан эсвэл
цайвар цэнхэр дэвсгэр». Тийм зурагт дүрсийн хүрээ нь Haar-аас илүү:

| | Haar cascade | Дүрсийн хүрээ |
| --- | --- | --- |
| Хажуу эргэлт, сүүдэр | Алддаг | Мэдрэг биш |
| Татах хэмжээ | opencv.js ~8MB | **0 байт** — дэвсгэрийн маскаас гарна |
| Юу олдог | Нүүр (хөмсөг→эрүү) | Толгойн **орой** — стандарт үүгээр хэмждэг |

Эмх замбараагүй дэвсгэрт дүрсийн хүрээ ажиллахгүй тул хөтөчийн `FaceDetector`
API (Chrome) руу шилжинэ, тэр ч байхгүй бол `null` буцааж гараар байрлуулахыг
шаардана.

### Хоёр нарийн алдаа, тэдгээрийг хэрхэн зассан

**1. Мөрийг толгой гэж үзэх.** «Дээд 40% доторх хамгийн өргөн мөр» гэж авбал
мөр (толгойноос 2–3 дахин өргөн) сонгогдоно. Засвар: оройноос доош явж, өргөн
нь дээд утгынхаа 60%-иас доош унасан цэгт (хүзүү) зогсоно.

**2. Дэвсгэрийн өнгө хүн рүү татагдах.** Цээж зурагт бие нь доод ирмэгийг
бараг бүтнээр эзэлдэг. Дөрвөн талыг жигд дунджилбал дэвсгэрийн өнгө бараан
болж, үерийн дүүргэлт **эсрэгээрээ** ажиллан бүх зургийг «дэвсгэр» гэж үзнэ.
Засвар: доод ирмэгийг алгасаж, дээд зурвас + хажуугийн дээд 60%-аас түүвэрлэнэ.

Хоёуланг нь `test/faceDetect.test.ts`, `test/idPhoto.test.ts` түгжсэн.

### Хуудасны байрлуулалт — бодит мөнгө

`sheetLayout()` нь хоёр чиглэлийг **хоёуланг** тооцож, олон багтаахыг сонгоно.
3×4 босоогоор 9, хэвтээгээр 8; 2×3 хэвтээгээр 18 багтдаг. Үлдсэн зайг тэнцүү
хуваан төвлүүлдэг тул нэг тал дээр цагаан зурвас үлдэхгүй.

## Нүүр хуудасны дэвсгэр зураг

Өмнөх хөх градиентийг **автоматаар солигддог зургууд** орлов
(`src/components/HeroSlideshow.tsx`). 6 секунд тутам зөөлөн шилжинэ.

**Зургаа солих:** `public/hero/` дотор файлаа хийгээд `src/data/site.ts` дэх
`HERO_IMAGES` жагсаалтыг засна.

```ts
export const HERO_IMAGES: readonly HeroImage[] = [
  { src: '/hero/1.jpg' },
  { src: '/hero/2.jpg' },
  { src: '/hero/3.jpg' },
];
```

⚠️ Одоо байгаа `1.svg`, `2.svg`, `3.svg` нь **орлуулга** — дэлгүүрийнхээ бодит
ажлын зургаар солино уу. «Энэ газар ямар чанартай ажилладаг вэ» гэдгийг
градиент биш, бодит ажил л хэлж чадна.

**Зураг бэлдэх зөвлөмж**

| Зүйл | Утга |
| --- | --- |
| Өргөн | 1920px орчим |
| Формат / чанар | JPEG, чанар 75–80 → нэг файл 200–300KB |
| Чиглэл | Хэвтээ. Босоо зураг гар утсан дээр хүчтэй тайрагдана |
| Найруулга | Гол зүйлээ ЗҮҮН доод хэсэгт битгий тавь — тэнд гарчиг, товч сууна |
| Тоо | 3–5. Олон байх тусам утасны сүлжээнд ачаа нэмнэ |

**Гүйцэтгэл, хүртээмжийн шийдлүүд** (тестээр түгжсэн):

- Зөвхөн **эхний зураг** `eager` + `fetchPriority="high"`, бусад нь `lazy` —
  бүгдийг зэрэг татвал анхны зурагдалт удаашрана
- Солилт нь `opacity` шилжилт — layout дахин тооцоологддоггүй тул хямд
- Таб нуугдсан үед таймер зогсоно (батарей)
- `prefers-reduced-motion` тохируулсан хүнд автомат солилт **ажиллахгүй**
- Дээр нь харанхуй давхарга — цайвар зураг дээр ч цагаан гарчиг уншигдана
- Жагсаалт хоосон бол градиент руу буцна, хуудас эвдрэхгүй
- Цэгүүд нь жинхэнэ `<button>` — гараар, дэлгэц уншигчаар ч сонгож болно

## Дүрс тэмдэг

`src/components/icons.tsx` — 20 орчим inline SVG. **Emoji хэрэглэхгүй.**

| Асуудал | Тайлбар |
| --- | --- |
| Төхөөрөмж бүр өөрөөр зурдаг | 🖼️ нь iOS, Android, Windows дээр гурван өөр хэлбэр, өнгөтэй — брэндийн харагдац алдагдана |
| Өнгө удирдагдахгүй | Emoji өөрийн өнгөтэй тул хажуугийн текстийн өнгөтэй нийцэхгүй, идэвхгүй төлөвт бүдгэрдэггүй |
| Заримд нь огт байхгүй | Хайрцаг (□) болж харагдана |
| Мөрийн өндөр үсэрдэг | Хэмжээ нь фонтоос хамаардаг |

Дүрсүүд нь `currentColor` ашигладаг тул `text-brand-500` гэх мэт класс шууд
ажиллана; хэмжээг `size-4` мэтээр өгнө. Lucide-ийн загвараар зурсан (24×24,
stroke 2) тул хожим тэр сан руу шилжихэд харагдац өөрчлөгдөхгүй.

**Icon сан суулгаагүй** шалтгаан: бидэнд 20 дүрс л хэрэгтэй, `lucide-react` нь
~40KB нэмнэ. Одоогийн файл 5.6KB (минифай хийхэд ~1.5KB).

`test/customer-web.test.ts` нь `src/` дотор emoji буцаж орохоос сэргийлнэ.

## Анхаарах

- **`@utility` vs `@layer components`** — Tailwind v4-д зөвхөн `@utility`-аар
  тодорхойлсон класс дээр `@apply` хийж болно. `.btn-accent { @apply btn … }`
  ажиллаж байгаа шалтгаан нь энэ.
- **Хэмжээ таних** — `lib/photoSize.ts` нь үйлчилгээний **нэрнээс** хэмжээг
  уншдаг. Каталогт шинэ мөр нэмэхдээ `10*15` эсвэл `А4` хэлбэрийг хадгална,
  эс бөгөөс 10×15 руу унана (`FALLBACK_SIZE`).
- **Зураг браузераас гардаггүй** — `PhotoEditor` нь object URL + canvas ашиглан
  зөвхөн дотоод preview үүсгэнэ. Файлыг сервер рүү илгээх бол `api/order.ts`
  дээр тусдаа upload зам нэмэх шаардлагатай.
- **Каталог хатуу бичигдсэн** — `src/data/catalog.ts` нь seed өгөгдлийн хуулбар.
  App дотор үнэ өөрчлөгдвөл энд тусахгүй. Хэрэв синк хэрэгтэй бол function
  дотроос `pmn/services`-ыг уншиж, кэшлэх нь зөв алхам.
# konica-web
